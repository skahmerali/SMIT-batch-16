// Storing data
// localStorage.setItem('username', 'Sheikh Ahmer');
// localStorage.setItem('userPreferences', JSON.stringify({ theme: 'dark', notifications: true }));

// // Retrieving data
// let username = localStorage.getItem('username');
// console.log(username)
// let userPreferences = JSON.parse(localStorage.getItem('userPreferences'));
// console.log(userPreferences.theme)

// var stdRecord = [];
// if (!database) {
// } else {
//   for (var i = 0; i <= database.length; i++) {
//     if (database[i].email === userEmail) {
//       let result = document.getElementById("result").innerText;
//       result = "email already exist";
//       return result;
//     }
//   }
// }

// now you create a function responsible for signup user
// create an object (signup details) push inside the array
// first get a localstorage value  and check weather user email is exist or not
// if email exist so show user already exist
// else save that array inside the localstorage

// now you create a function responsible for login user
// get a locastorage item and save in the varriable
// implement the loop and find the user email
// if user exist so check the password
// if passowrd and email match so show the alert of sucessfully message
// then show the dashboard / profile / home through window.location.href = "./about.html"

// homepage/ dashboard/ profile

// create a function responsible for shows the blog
// get the blogs from localstorage
// show the create blog button
// create an empty array (blogsDatabase)
// create a function where add bloogs object
// {
//  name: '',
//  title: '',
// blogBody : '', 
//  blogImg : '', (optional)
// date: '', 
// author : '',
// category : '',
// }
// then push this obj in the array and save it inside the localstorage 

